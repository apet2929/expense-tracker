import { useState } from "react/cjs/react.production.min";


function TransactionTableElement(props) {
    let id = useState(props.id);
    let date = useState(props.date);
    let amount = useState(props.amount);
    let description = useState(props.description);
    let category = useState(props.category);
}